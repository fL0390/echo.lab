{
  "name": "echo-proxmox-proxy",
  "version": "1.0.0",
  "description": "WebSocket proxy for echo → Proxmox noVNC console",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js"
  },
  "dependencies": {
    "http-proxy": "^1.18.1"
  }
}
